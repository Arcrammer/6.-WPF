 var integerWithinString = "6";
 var compose = 7 + Number(integerWithinString); // Composes 'integerWithinString' value as an integer with the string '7' after converting it to an integer
 
 // What is the difference between Number() and toInt()?
 
 document.write(compose);